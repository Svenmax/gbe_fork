# GC 重构后续实施任务清单

## 推进索引

按阶段推进，完成当前阶段的必要任务和验证后再进入下一阶段。标记为 `*` 的任务是增强项，可在必要任务完成后执行。

### Stage 0：基线保护和清理

- 覆盖任务：6、11、14、16。
- 目标：确认验证入口稳定，减少构建噪音，保证 Premake 可选测试工程和 shell 离线测试源列表一致。
- 必要验证：`tools/run_gc_offline_tests.sh`、`tools/run_gc_offline_tests.sh --full`、`git diff --check`。
- 具备工具环境追加验证：`premake5 --with-gc-tests gmake2`。
- 完成标准：默认/full 离线测试通过，Premake GC test target 仍为可选生成，任务清单记录残余平台验证风险。

### Stage 1：Inventory action-list pilot

- 覆盖任务：1。
- 目标：在 `GBE_HandleDotaEquipItemsRequest` 上验证 pure planner + explicit executor 模式。
- 必要前置：equip basic、empty、full forward 测试能证明 response、save、server GC forward、network broadcast、snapshot refresh 的顺序。
- 必要验证：默认/full 离线测试和 `git diff --check`。
- 完成标准：planner 不触达 coordinator/network/file/server GC，executor 显式保持原副作用顺序，相关 reason/job/session 断言保持通过。

### Stage 2：测试覆盖加深和 harness 收缩

- 覆盖任务：2、3、4、13.4。
- 目标：加深 7034、lobby lifecycle、launch loading 路径测试，同时控制 `stubs.h` 膨胀。
- 必要前置：新增测试只需要 recorder 字段或轻量 stub 扩展即可表达行为。
- 必要验证：默认/full 离线测试和 `git diff --check`。
- 完成标准：match/lobby/chat 关键顺序、payload 关键字段、reason 稳定性具备测试保护，harness 分区清晰。

### Stage 3：Lobby 和 launch state decision 收口

- 覆盖任务：8、9。
- 目标：把 abandon、teardown、launch、reconnect 的状态判断收口到无副作用 decision helper 和函数级 state access。
- 必要前置：对应路径已有顺序测试和 state mutation 时机断言。
- 必要验证：默认/full 离线测试、`git diff --check`，涉及声明迁移时执行 `python3 tools/_audit_gc_refactor.py`。
- 完成标准：decision helper 只返回结构化结果，handler/executor 继续显式执行 response、publish、pending flag 和外部副作用。

### Stage 4：Protocol DTO 和 routing 收口

- 覆盖任务：7、10。
- 目标：减少裸 wire 字段读取和 direct/wrapped routing 重复 adapter。
- 必要前置：DTO parse 边界和 routing fallback 具备 focused tests。
- 必要验证：默认/full 离线测试、`git diff --check`，涉及新增 helper 或声明迁移时执行审计脚本。
- 完成标准：7034、7070、8052、8053、7035 使用结构化 request/response 输入，unsupported emsg 和 job/session 透传保持测试保护。

### Stage 5：Dependency seam、persistence 和 template 治理

- 覆盖任务：12、15。
- 目标：收敛 save、network、server GC、lobby publish 等外部依赖边界，并治理 template/replay ownership。
- 必要前置：外部依赖触点和 template/replay 常量 ownership 已盘点。
- 必要验证：默认/full 离线测试、`git diff --check`，template 变更需有 focused tests 或 fixture 对齐。
- 完成标准：pure helper 不写文件、不发送网络、不触发 server GC；canned payload 的用途、patch 点和兼容风险可追踪。

### Stage 6：持续治理和 CI 固化

- 覆盖任务：5、11、14。
- 目标：在前面阶段稳定后继续收缩 internal header、固化验证 pipeline 和平台构建等价性。
- 必要前置：测试覆盖能保护被迁移的 helper 或 header 边界。
- 必要验证：默认/full 离线测试、`git diff --check`、`python3 tools/_audit_gc_refactor.py`，具备工具环境执行 Premake gate。
- 完成标准：新增文件和声明迁移进入正确构建入口，默认 CI 成本保持可控，可选 GC test path 可验证。

## 执行规则

- 每轮只处理一个 stage 中的一个必要任务或一个增强任务。
- 测试补强和生产逻辑重构分开提交。
- 任务完成时勾选对应条目，并在条目下记录实际验证命令。
- 出现顺序、job/session、payload、reason 或 publish 时机变化时，先停在测试诊断，不继续扩大范围。
- 如果某个任务需要显著扩大 stub 行为，先拆小任务补 wire/helper 测试或 recorder 字段。

- [x] 1. Inventory action-list pilot
  - [x] 1.1 记录 `GBE_HandleDotaEquipItemsRequest` 当前副作用顺序
    - 目标文件：`dll/gbe_dota_inventory_handlers.cpp`
    - 对齐 `tools/gbe_dota_handler_test/smoke_test.cpp` 中 equip basic、empty、full forward 测试
    - 明确 SO update、2570 response、save、server GC forward、network broadcast、lobby snapshot refresh 的顺序约束
    - 当前顺序：parse failure 直接返回且无副作用；modified 路径为 SO UpdateMultiple(26) -> local response(2570) -> save -> server GC full cache -> server GC create(21) -> server GC update(26) -> network broadcast -> lobby snapshot refresh
    - 测试对齐：`test_inventory_equip_basic` 覆盖 26 -> 2570 -> save；`test_inventory_equip_empty` 覆盖 parse failure 无副作用；`test_inventory_equip_full_forward` 覆盖完整 8 步顺序和 full cache 在 server GC 21/26 前
    - 约束：local response(2570) 必须早于 server GC forward、network broadcast、lobby snapshot refresh；server GC full item cache 必须早于 emsg 21/26；direct equip response 保持 unwrapped 且无 `GBE_PushDotaResponse` reason metadata
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 1.2 为 equip 请求定义局部 plan 数据结构
    - 放在 `dll/gbe_dota_inventory_handlers.cpp` anonymous namespace
    - 复用 `GBE_DotaActionType` 和 `GBE_DotaActionList`
    - plan 字段覆盖 modified item ids、response body、cache version、server forward 参数、broadcast/snapshot reason
    - 结果：新增 `EquipItemsPlan` 和 `EquipItemsServerForwardPlan`，字段覆盖 modified item ids、response body、cache version、server forward、broadcast、snapshot reason 和有序 `GBE_DotaActionList`
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 1.3 提取 equip pure planner
    - planner 输入为请求 body、当前 item 列表、lobby/server 状态快照和 source job
    - planner 输出 item mutation 结果和有序 action list
    - planner 不调用 coordinator 方法、不访问 `get_steam_client()`、不发送消息、不保存文件
    - 结果：新增 `EquipItemsPlanningContext` 和 `plan_equip_items_request`，planner 基于请求 body、items 快照、cache version 和运行时快照输出 item mutation 结果、response body、server forward/broadcast/snapshot 意图和有序 action list；当前未接入 handler 执行路径，保留 1.4 切换
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 1.4 将 coordinator equip handler 改为执行 plan
    - coordinator 负责应用 item mutation、push response、save、server GC forward、network broadcast、snapshot refresh
    - 保持现有日志、reason、payload、job/session 行为
    - 保持当前 handler tests 全部通过
    - 结果：`GBE_HandleDotaEquipItemsRequest` 现在基于 `EquipItemsPlan` 应用 item mutation、执行 SO update、local response、save、server GC forward、network broadcast 和 lobby snapshot refresh；update payload 构造保留在 coordinator 层，planner 保持无 coordinator 方法调用和无 I/O 副作用
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x]* 1.5 补 equip planner focused tests
    - 覆盖空请求、单 item equip、多 item equip、item missing、style bitmask 输入
    - 验证 plan 中 local response 位于外部副作用之前
    - 结果：新增 planner bridge 和 5 个 focused tests，覆盖 parse failure、单 item 顺序、多 item 外部副作用顺序、missing item、style index 输入；handler smoke test 总数增至 26
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过

- [x] 2. 扩展 match 7034 真实路径测试
  - [x] 2.1 构造 7034 connected player 请求 body
    - 目标文件：`tools/gbe_dota_handler_test/smoke_test.cpp`
    - 使用现有 varint/length-delimited helper 构造真实 wire body
    - 断言 member runtime state、hero id、response payload 和 source job
    - 结果：新增 `make_dota7034_connected_player_body` 和 `test_match_7034_connected_player_updates_runtime_before_response`，覆盖 field 1 connected player sub-message 的 steam id / hero id 解析、runtime state mutation、owner hero 更新，以及 `7034_connected_player` publish -> emsg 26 runtime update -> emsg 7034 response 的顺序
    - 说明：7034 response payload 由现有 adapter 构造，当前通用 `read_header_jobs` 不适用于该 payload 格式；测试锁定 response emsg 和 payload 非空，避免错误解析 source job
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 2.2 构造 7034 disconnected player 请求 body
    - 覆盖 disconnected player 导致的 member runtime state 更新
    - 断言 queued lobby update 或 response 的顺序
    - 结果：新增 `make_dota7034_disconnected_player_body` 和 `test_match_7034_disconnected_player_updates_runtime_before_response`，覆盖 field 7 disconnected player sub-message 及 nested leaver state field 3，断言 runtime state 标记 disconnected、hero id 清零、`7034_disconnected_player` publish 在 emsg 7034 response 前执行
    - 说明：测试使用已进入 state 2 / game_state 10 的 lobby 隔离 launch poll fallback，聚焦 disconnected player 分支自身的 publish/response 顺序
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 2.3 覆盖 7034 game_state runtime update
    - 构造包含 game_state/send_reason 的请求
    - 断言 26 runtime lobby details update 的 emsg、source job 和 payload 非空
    - 结果：新增 `make_dota7034_game_state_body` 和 `test_match_7034_game_state_runtime_update_before_response`，覆盖 field 2 game_state 与 field 8 send_reason 的真实 wire body，触发 AP hero_selection fallback strategy_time 分支
    - 断言：game_state-only 请求不触发 member runtime mutation；emsg 26 runtime update 先于 emsg 7034 response；runtime update payload 非空并保留 strategy-time reason；lobby state 保持 2 且 game_state 推进到 3
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 2.4 覆盖 7034 launch poll 最小路径
    - 设置 `GBE_local_lobby.launch_phase` 为 run queued 或 loaded 前状态
    - 断言 launch poll 不破坏 7070/8052/8053 已有顺序测试
    - 结果：新增 `RecordedPracticeLobbyDetailsUpdate` recorder 字段和 `test_match_7034_launch_poll_records_details_update_before_response`，覆盖 active match lobby 在 state 1 / game_state 0 / run queued launch phase 下的 7034 空 body launch poll 分支
    - 断言：launch poll 触发一次 `GBE_SendDotaPracticeLobbyDetailsUpdate(false, nullptr, "7034_launch_poll")`，不触发 member runtime mutation，仍输出 emsg 7034 response，且 lobby state/game_state 保持不变；已有 7070/8052/8053 顺序测试继续通过
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过

- [x] 3. 扩展 lobby lifecycle 顺序测试
  - [x] 3.1 为 7042 leave lobby 添加最小 handler 测试
    - 覆盖 response、CacheUnsubscribed、shared state publish 或 reset 标志
    - 断言 reason 和 session/wrapped 保留
    - 结果：新增 `test_lobby_leave_queues_25_then_clears_local_lobby`，覆盖 active non-runtime lobby 的 7042 leave 路径，断言 emsg 25 CacheUnsubscribed response、wrapped/session 保留、reason 为 `7040_leave_25`、payload 非空，以及 response 后本地 lobby/generic lobby/chat channel 状态清空
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 3.2 为 8246 destroy lobby 添加最小 handler 测试
    - 覆盖 host destroy 路径的 response 和 state cleanup
    - 断言 lobby id、pending reset 或 suppress 标志
    - 结果：新增 `test_lobby_destroy_queues_25_then_8247_and_clears_lobby`，覆盖 active lobby destroy 路径在携带 request job 时的双 response 顺序，以及返回前清空 lobby/generic lobby/chat channel 状态
    - 说明：8246 当前通过 outbound router 推送外层 `GBE_kEMsgClientFromGC` envelope；测试锁定两个 ClientFromGC envelope 的顺序和 payload 非空，避免误把外层 emsg 当作内层 25/8247 解析
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 3.3 为 7047 kick 添加最小 handler 测试
    - 覆盖 kick target 解析和 response/publish 顺序
    - 使用 stub 记录 target steam id 或 account id
    - 结果：实际 kick handler 为 `GBE_HandleDotaPracticeLobbyKickRequest` / emsg 7081，7047 对应 set team slot；新增 `RecordedLobbyKick` 和 `test_lobby_kick_removes_member_then_publishes_details`，覆盖 field 3 target account id 解析、account id 到 steam id 映射、matchmaking kick 调用目标、目标 member slot 清空、`7081_kick_member` publish 和 details update reason/session 透传
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 3.4 为 7050 set details 添加最小 handler 测试
    - 覆盖 lobby metadata mutation 发生在 details update publish 前
    - 断言 room name、server region 或 custom game 字段
    - 结果：实际 set details handler 为 `GBE_HandleDotaPracticeLobbySetDetailsRequest` / emsg 7046；新增 `make_lobby_set_details_body` 和 `test_lobby_set_details_mutates_before_publish_and_details_update`，覆盖 room name、server region、game mode 解析与本地 lobby mutation，断言 `7046_set_details` publish 和 details update reason/session 透传
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过

- [x] 4. 收缩 handler test harness 边界
  - [x] 4.1 整理 `stubs.h` 内部 section
    - 按 core、inventory、chat/lobby、match、free globals 分区
    - 只移动声明和注释，不改变行为
    - 结果：仅更新 `tools/gbe_dota_handler_test/stubs.h` 的 section 标题和 coordinator 内部 hook 分组注释，标明 core type surface、core service stubs、recorder、inventory protobuf、coordinator seam、inventory/match/lobby hooks、platform service stubs；未移动声明或改变行为
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 4.2 抽出可复用 wire body builder helper
    - 目标文件：`tools/gbe_dota_handler_test/smoke_test.cpp`
    - 统一 varint、fixed32、fixed64、length-delimited、nested message 构造
    - 保持现有测试输入字节不变
    - 结果：新增 `WireBodyBuilder`，封装 varint、bytes、nested message 构造；迁移 equip op、7034 connected/disconnected/game_state、lobby set details、chat join/leave、kick、rank、7070、8052、8053 等测试 body 构造调用；底层 `encode_*` helper 保留给现有直接构造和 builder 实现复用
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 4.3 评估是否拆分 stub header
    - 如果 `stubs.h` 继续超过可维护范围，拆成 core/inventory/lobby/match header
    - 每次拆分只移动声明，不新增测试语义
    - 结果：当前 `stubs.h` 约 1073 行，已通过 4.1 建立 core type surface、service stubs、recorder、coordinator seam、domain hooks、platform service stubs 分区；立即拆分会增加 include/forward declaration 顺序风险，收益低于单独移动成本
    - 决策：暂缓拆分文件，后续当新增 domain stub 继续增长或出现独立编译单元需求时，再以独立小任务只移动声明到 core/inventory/lobby/match header
    - 验证：`git diff --check` 通过
  - [x] 4.4 为 ActionRecorder 增加字段读取 helper
    - 封装 emsg mask、header job 读取、payload 非空断言
    - 降低 smoke test 重复代码
    - 结果：在 `tools/gbe_dota_handler_test/smoke_test.cpp` 增加 `action_emsg`、`expect_push_action`、`expect_push_payload`，复用已有 `read_header_jobs`；替换 inventory、chat/lobby lifecycle、7034 match-flow 测试中的重复 push/emsg/payload 断言
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过

- [x] 5. 收缩 `gbe_dota_gc_internal.h` 共享边界
  - [x] 5.1 盘点当前 internal header 声明
    - 按 wire payload、item payload、lobby payload、stateful orchestration、logging、extern data 分类
    - 输出保留、迁移、删除清单到本任务文档或代码注释
    - 结果：当前 header 约 572 行，声明可分为以下类别：logging/diagnostic 为 `GBE_GC_DebugLog`、`GBE_DescribeDotaLaunchPhase`、`GBE_LogDotaSOCacheSubscribedSummary`、`GBE_LogDotaResponsePacket`、`GBE_LogGCProtoBoundary`；extern data/state 为 reconnect cache、shared lobby state、loot data、old Dota replacement tables、Steam ticket constant、server hello cache、practice lobby template constants；wire payload 为 account/steam/lobby id patch helpers、direct replay prepare、hello context extract/build/compose、welcome body builder；lobby payload 为 practice lobby launch/cache/details replay/build/adapt/compose helpers、custom games list adapt、other-left-channel detector；item payload 为 `GBE_DotaEquipOp`、equip parser、unlock style bitmask、SO single object/econ item serialization；stateful orchestration 为 server GC equipped-item cache forward plus shared lobby/server hello mutable caches
    - 保留清单：短期保留跨 TU 仍在调用的 stateful orchestration、extern data/state、logging helpers，以及当前仍承担跨 TU 桥接的 lobby replay/build Impl declarations
    - 迁移候选：wire payload 可收敛到更窄的 wire/helper header；item payload 可收敛到 item helper header；lobby payload 可收敛到 lobby helper header；`ser_var`/`deser_var` 可在确认只服务 payload/wire 后移入更窄 header 或定义 TU
    - 删除候选：本轮未确认 stale declaration；后续 5.3 结合调用点审计再删除只剩 file-local 使用的声明
    - 验证：`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 5.2 为 payload helper 建立更窄 header
    - 视调用点拆分或复用 `gbe_dota_payload_wire_helpers.h`、`gbe_dota_payload_item_helpers.h`、`gbe_dota_payload_lobby_helpers.h`
    - 更新调用方 include
    - 结果：新增 `dll/gbe_dota_payload_item_helpers.h`，迁出 `GBE_DotaEquipOp`、`GBE_ParseDotaEquipOps`、`GBE_ApplyDotaUnlockStyleBitmask`、`GBE_BuildSOSingleObjectFromItem`、`GBE_SerializeEconItemToGcprotobuf` 声明；`gbe_dota_gc_internal.h` 移除 item payload 声明；实际调用方 `gbe_dota_payload_item_helpers.cpp`、`gbe_dota_inventory_handlers.cpp`、`gbe_dota_template_replay_handlers.cpp`、`gbe_dota_inventory_coordinator.cpp` 和 payload helper test 显式 include 新 header
    - 决策：本轮先落地 item helper header，wire/lobby payload helper 的声明迁移保留给后续更小批次，避免一次性移动高参数量 lobby replay/build declarations
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 5.3 下沉 file-local helper 声明
    - 能进入 anonymous namespace 的 helper 不留在 internal header
    - 删除迁移后失去调用点的 stale declarations
    - 结果：本轮迁出 item payload 声明后，继续检查 wire/lobby/top-custom-games/other-left-channel 等声明；相关 helper 仍有跨 TU 调用，例如 lobby payload helpers、snapshot coordinator、template replay handlers、welcome coordinator 与 `steam_game_coordinator.cpp` 仍依赖这些 declarations
    - 决策：本轮不做 file-local 下沉或删除；`python3 tools/_audit_gc_refactor.py` 确认没有 zombie declarations 和 under-exposed definitions，后续迁移 wire/lobby helper header 后再重新评估
    - 验证：`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 5.4 运行审计验证
    - 执行 `python3 tools/_audit_gc_refactor.py`
    - 执行 `tools/run_gc_offline_tests.sh --full`
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过

- [x] 6. 清理编译警告和构建配置验证
  - [x] 6.1 修复 payload helper `extern const char *` 初始化警告
    - 目标文件：`dll/gbe_dota_gc_payload_helpers.cpp`
    - 同步 header declaration 和 definition 的 const-correctness
    - 保持 payload helper tests 通过
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 6.2 校验测试脚本和 Premake 源列表一致
    - 对比 `tools/run_gc_offline_tests.sh` 和 `premake5.lua` 中两个 test target 的源列表
    - 确保新增测试依赖同时进入 shell 和 `--with-gc-tests` Premake 配置
    - 结果：两个 GC test target 的 `.cpp` 源列表一致；Premake 额外列出 headers 和 `pb_stubs/**.h` 用于工程生成
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过
  - [x] 6.3 在具备工具环境验证 Premake 生成
    - 执行 `premake5 --with-gc-tests gmake2`
    - 验证 `tool_gbe_dota_gc_payload_helpers_test` 和 `tool_gbe_dota_handler_test` 工程存在
    - 当前环境：`command -v premake5` 无输出，无法执行 Premake 生成；该项作为环境阻塞记录收尾，具备工具环境时执行上述命令复验
  - [x] 6.4 精简 touched files include
    - 只处理本轮修改过的 domain 文件和 test harness 文件
    - 避免跨领域批量 include 清理造成审查困难
    - 结果：移除 `tools/gbe_dota_handler_test/free_func_stubs.cpp` 中未使用的标准库 include；生产 payload helper include 保持不变，避免扩大清理范围
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过

- [x] 7. Post-login routing 收口
  - [x] 7.1 盘点 direct/wrapped routing 重复分支
    - 目标文件：`dll/gbe_dota_post_login_handlers.cpp`
    - 输出 simple adapter、special adapter、fallback 三类清单
    - 保留 7034、template replay、server assignment 等复杂路径的显式 adapter
    - 结果：direct 入口先解析 `GBE_DirectProtoContext` 并构造 `DotaGcRequestContext`，wrapped 入口先 `extract_wrapped_post_login_request`，二者均先尝试 `GBE_DispatchDotaPostLoginRequest`
    - simple adapter 候选：direct 7070/8052/8053、1087 AddSocket、2571 UnlockItemStyle、2577 SetItemStyle、8727/8886/8793 minimal success、7427 notifications、4523 upload rate、7534 profile card、2581 lookup account、7503 emoticon data、8095 conduct scorecard、8800 coaching summary、8879 rank、7450 batch resources、2569 equip items；wrapped 7272 leave chat、8246 destroy lobby、7047 set team slot fallback
    - special adapter：direct 7273 chat message、7272 leave chat、8246 destroy lobby、7035 abandon current game、7072 leaver detected、7003 sign-out permission、7004 sign-out、4506/5429 launch advance, 8870 launch marker, 4511 LAN server available；wrapped 7035 abandon、7004 sign-out、7070/8052/8053 custom-game launch lifecycle、8009 SourceTV list、7091 watch game
    - explicit complex path 保留：7034 connected-player/runtime flow、4508 server assignment, template replay fallback, late Steam chain 5410/5432 consume branches, unsupported wrapped request prefilter
    - fallback：direct fallback 为 `GBE_HandleDotaTemplateReplayRequest(request_emsg, body, body_size, has_source_job, source_job)`；wrapped fallback 在未匹配 explicit branch 时进入 `GBE_HandleDotaPracticeLobbySetTeamSlotRequest(...)`
    - 验证：`git diff --check` 通过
  - [x] 7.2 扩展统一 request context
    - 确认 `DotaGcRequestContext` 或等价结构包含 emsg、body、wrapped、outer session、source job、target job、request path
    - 避免 handler 直接重新解析路由层上下文
    - 结果：`DotaGcRequestContext` 新增 `target_job_id`、`has_target_job` 和 `DotaGcRequestPath path`；direct post-login 入口从 `GBE_DirectProtoContext::protohdr` 填充 source/request job、target job 和 `Direct` path；wrapped extraction 填充 target job 和 `Wrapped` path；`GBE_DotaWrappedDirectContext` 桥接结构同步新增 target job 字段以保留上下文信息
    - 范围：现有 dispatch adapter 仍沿用 `request_job_id`/`has_request_job`，本次仅补齐上下文字段，保持 handler 行为不变
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 7.3 将简单 direct handler 迁入 routing registry
    - 优先迁移 misc minimal response、rank/profile、inventory simple request-response 路径
    - 保持日志字段和 fallback 返回语义
    - 结果：将 3 个已有 handler smoke test 覆盖的 direct-only misc 路径迁入 `GBE_DispatchDotaPostLoginRequest` registry：7427 notifications、4523 upload rate、8879 rank；新增 adapter 使用 `DotaGcRequestPath::Direct` guard，避免 wrapped 路径误入；从 `GBE_HandleDotaDirectPostLoginRequest` 移除对应 if 分支
    - 决策：profile/inventory/simple minimal 其余路径本轮保留在 direct 显式分支，等待 7.4 registry tests 后再继续迁移
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 7.4 增加 routing registry tests
    - 验证 unsupported emsg 返回 false
    - 验证 wrapped/direct 同 emsg adapter 保持 body、job、session 透传
    - 结果：增强 `tools/_audit_gc_refactor.py` 的 post-login dispatch audit，除原 16 个 wrapped/direct lobby registry entries 外，新增校验 7427、4523、8879 三个 direct-only registry entries；校验内容包括 table entry、adapter 调用目标以及 `DotaGcRequestPath::Direct` guard，防止 direct-only adapter 被 wrapped path 误用
    - 范围：当前 handler test harness 未直接编译 `steam_game_coordinator.cpp`，因此本轮用审计型 focused test 锁定 registry 表结构和 adapter guard；已有 handler smoke tests 继续覆盖 7427/4523/8879 实际响应行为
    - 验证：`python3 tools/_audit_gc_refactor.py` 通过并报告 19 个 dispatch entries；`tools/run_gc_offline_tests.sh --full` 通过；`git diff --check` 通过

- [x] 8. Lobby state machine 集中化
  - [x] 8.1 扩展 abandon/teardown transition decision
    - 基于现有 `compute_abandon_decision` 模式
    - 覆盖 7035、7014、25、pending reset/finalize 的 decision fields
    - helper 不发送消息、不写全局状态
    - 结果：扩展 `AbandonDecision`，把 7035 的 queue 25、pending reset、discard queued launch、suppression、postgame teardown、wrapped session 和 postgame 25/7010 输出意图集中到 pure decision；新增 `TeardownRetrievalDecision` 和 `compute_teardown_retrieval_decision`，把 RetrieveMessage 中 7014 abandon finalize、25 normal signout finalize、25 deferred reset 的判断收口到 pure helper
    - 行为保持：handler 和 coordinator 仍按原顺序执行真实副作用；arcade launch failed path 继续只 queue 25 并跳过 postgame；current-game disconnect 继续 queue 25 并设置 pending reset；ready abandon 继续通过 teardown queue postgame 25/7010；RetrieveMessage 仍在 pop 后执行 finalize/reset
    - 测试：扩展 `tools/gbe_dota_lobby_state_test/gbe_dota_lobby_state_test.cpp`，覆盖 current-game disconnect decision fields、ready teardown decision fields、arcade failed decision fields、7014 channel match finalize、25 signout finalize、25 pending reset 和 non-Dota profile guard
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 8.2 提取 launch lifecycle transition decision
    - 覆盖 7041、7070、8052、8053、7034 runtime game_state
    - 输出 next state、next game_state、launch phase、publish reason
    - 结果：新增 `LaunchLifecycleTransitionDecision`，将 7070 ready-up、8052 started-loading、8053 finished-loading、7034 custom runtime game_state 和 7034 launch poll 的 next state、next game_state、launch phase、publish/details/runtime-update reason 收口到 pure helper；7041 继续复用既有 launch init/event plan，并补充 focused test 锁定 initial details 和 presence intent
    - 行为保持：direct/wrapped 7070 仍先发送 7170 response，再按 decision 执行 state mutation、publish 和 details update；8052 仍在字段更新后尝试 RUN runtime update，失败时回退 publish/details；8053 仍在 success path 标记 Loaded、更新 local member runtime、publish local member data 后 publish/details；7034 runtime game_state 和 launch poll 继续由 coordinator 发送真实 26 或 details update
    - 测试：扩展 `tools/gbe_dota_lobby_state_test/gbe_dota_lobby_state_test.cpp`，覆盖 7041 event intent、7070 valid/invalid transition、8052 run/fallback transition、8053 success/failure transition、7034 runtime game_state 和 launch poll terminal guard
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 8.3 提取 reconnect eligibility decision
    - 覆盖 recent reconnect context、server id、owner connected、launch phase
    - 保持现有 reconnect payload 行为
    - 结果：新增 `ReconnectEligibilityDecision` 和 `ReconnectInterceptionDecision`，将 shared/recent reconnect context eligibility、server id/connect/started 判断、owner connected 与 launch phase 记录、P2P rendezvous gate 和 PostConnectionStateMsg direct-connect gate 收口到 pure helper
    - 行为保持：`build_reconnect_context` 与 `GBE_GetDotaReconnectContext` 继续输出相同 reconnect payload；`SendP2PRendezvous` 仍保持 atomic `GBE_dota_reconnect_eligible.compare_exchange_strong` 原有执行位置；PostConnectionStateMsg 仍保留 arcade/recovery、eligible、owner guard 与 direct socket/callback 顺序
    - 测试：扩展 `tools/gbe_dota_lobby_state_test/gbe_dota_lobby_state_test.cpp`，覆盖 valid/invalid shared context、recent context 等价条件、missing server id、missing connect、not-started context、owner connected/launch phase flags、remote mismatch、local owner、ineligible atomic flag、non-arcade context 和 missing context
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x]* 8.4 为 transition helpers 添加 focused tests
    - 覆盖 valid/invalid transition、stale lobby、owner disconnect、launch failed before connect
    - 结果：focused coverage 已分布在 8.1、8.2、8.3 的 lobby state tests 中；覆盖 current-game disconnect、ready teardown、arcade failed、valid/invalid launch lifecycle、terminal launch poll guard、valid/invalid reconnect context、owner connected/owner disconnect eligibility、missing server/connect、ineligible atomic flag 和 non-arcade guard
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过

- [x] 9. Coordinator 和 global state 解耦
  - [x] 9.1 盘点 Dota GC extern/global state 读写点
    - 覆盖 shared lobby、pending reset、recent reconnect、launch flags、host showcase flags
    - 输出按状态分组的读写调用点清单
    - 结果：当前 Dota GC 可变状态分为 coordinator 成员状态和进程级 extern/global 状态；本轮仅盘点调用点，不做封装或迁移
    - `GBE_local_lobby`：声明在 `dll/dll/steam_game_coordinator.h:110`；读写分散在 `steam_game_coordinator.cpp`、`gbe_dota_lobby_state_coordinator.cpp`、`gbe_dota_lobby_handlers.cpp`、`gbe_dota_match_handlers.cpp`、`gbe_dota_post_login_handlers.cpp`、`gbe_dota_chat_handlers.cpp`、`gbe_dota_lobby_launch_coordinator.cpp`、`gbe_dota_lobby_flow_coordinator.cpp`、`gbe_dota_lobby_snapshot_coordinator.cpp`、`gbe_dota_connection_lifecycle.cpp`、`gbe_dota_inventory_handlers.cpp`、`gbe_dota_inventory_coordinator.cpp`、`gbe_dota_network_callbacks.cpp`、`gbe_dota_template_replay_handlers.cpp`、`gbe_dota_misc_handlers.cpp`、`gbe_dota_welcome_coordinator.cpp`；主要写入点为 create/join/launch/set details/set team、7041/7070/8052/8053/7034 runtime flow、chat/broadcast channel lifecycle、generic lobby restore/adopt、postgame teardown、owner connection lifecycle 和 ResetGCMemory/clear runtime
    - `GBE_shared_dota_lobby_state`：extern 声明在 `dll/gbe_dota_gc_internal.h:48`，定义在 `dll/steam_game_coordinator.cpp:60`；写入点集中在 `GBE_PublishSharedDotaLobbyState` 的 `publish_local_lobby_to_shared`、`clear_dota_runtime_state`/postgame finalize reset、`GBE_PublishDotaPracticeLobbyMetadata` 的 connect/server_id 回写、`GBE_TrySyncDotaLobbyServerIdFromGameServer` 的 server_id 回写、post-login runtime connect 回写；读取点集中在 reconnect payload helper、template replay/source TV、wrapped post-login SourceTV/watch-game、shared-to-local restore、owner fallback 和 suppressed abandoned lobby guard
    - Pending/reset/finalize flags：`GBE_pending_reset_after_cache_unsubscribed`、`GBE_pending_reset_after_cache_unsubscribed_lobby_id`、`GBE_pending_dota_abandon_finalize_after_7014`、`GBE_pending_dota_abandon_finalize_lobby_id`、`GBE_pending_dota_normal_signout_finalize_after_25`、`GBE_pending_dota_normal_signout_finalize_lobby_id`、`GBE_suppressed_dota_abandon_lobby_id` 声明在 `dll/dll/steam_game_coordinator.h:99-105`；写入点为 7035 current-game/failed launch abandon、7272 abandon channel leave、7004 signout、postgame teardown queue、ResetGCMemory、clear runtime 和 suppression mark/clear；读取点为 `RetrieveMessage` teardown decision、suppression guard、discard queued launch guard
    - Recent reconnect state：`GBE_recent_dota_reconnect_context_valid` 和 `GBE_recent_dota_reconnect_context` extern 声明在 `dll/gbe_dota_gc_internal.h:43-44`，定义在 `dll/steam_game_coordinator.cpp:61-62`；写入点为 `GBE_PublishSharedDotaLobbyState` 成功构造 reconnect context、lobby create 初始化清空、clear runtime 清空；读取点为 `GBE_GetDotaReconnectContext` 的 shared fallback
    - Reconnect eligibility atomic：`GBE_dota_reconnect_eligible` extern 声明在 `dll/dll/gbe_dota_reconnect_shared.h:34`，定义在 `dll/steam_game_coordinator.cpp:66`；读取点为 `steam_networking_socketsserialized.cpp`、`steam_user.cpp`；写入点为 `SendP2PRendezvous` compare-exchange 消费 eligibility、`steam_user.cpp` 的 CancelAuthTicket/GetAuthSessionTicket reconnect reset、lobby create 初始化 reset、direct connect callback 对非 arcade launch 清空
    - Launch/sync flags：`GBE_dota_login_sync_sent`、`GBE_dota_private_lobby_snapshot_replayed`、`GBE_last_dota_launch_state_pushed_game_state`、`GBE_last_dota_launch_persona_signature`、`GBE_last_dota_direct_connect_callback_signature`、`GBE_last_lobby_poll_time` 声明在 `dll/dll/steam_game_coordinator.h:95-108`；读写点分别集中在 welcome login sync、private lobby snapshot replay guard、launch state peer push dedupe、rich presence persona dedupe、direct connect callback dedupe、7034 launch poll throttle；reset 点分散在 shutdown/clear runtime、generic/shared restore、inventory equip broadcast、snapshot invalidation、direct connect flow 和 launch finalize
    - Host showcase flag：`GBE_dota_host_showcase_equip_pushed` 声明在 `dll/dll/steam_game_coordinator.h:96`；写入点为 shutdown reset 和 `GBE_HandleDotaDirect7034Response` 在 TEAM_SHOWCASE repush 成功后置 true；读取点为同一 7034 response guard
    - Server hello cache：`GBE_last_dota_server_hello_context` extern 声明在 `dll/gbe_dota_gc_internal.h:213`，定义在 `dll/steam_game_coordinator.cpp:80`；写入点为 direct ServerHello 解析成功；读取点为 `gbe_dota_welcome_coordinator.cpp` 的 server welcome replay
    - Loot data：`GBE_vpk_loot_data` extern 声明在 `dll/gbe_dota_gc_internal.h:82`，定义在 `dll/steam_game_coordinator.cpp:63`；写入点为 welcome/schema load 后 move 赋值；读取点为 `gbe_dota_template_replay_handlers.cpp` 的 opening treasure/tool/bundle replay
    - Networking reconnect dedupe globals：`GBE_last_post_connection_state_server_id`、`GBE_post_connection_state_retry_count`、`GBE_last_post_connection_state_size`、`GBE_last_post_connection_state_callback_server_id`、`GBE_last_post_connection_state_callback_endpoint`、`GBE_last_direct_connect_server_id`、`GBE_last_direct_connect_endpoint` 定义在 `dll/steam_networking_socketsserialized.cpp:292-298`；读写点局限于 `PostConnectionStateMsg` 的 direct socket/connect callback dedupe
    - 后续拆分建议：9.2 优先封装 pending/reset/finalize flags，因为读写点少且已经由 teardown decision 保护；9.3 封装 recent reconnect context 和 reconnect eligibility atomic；`GBE_local_lobby` 与 `GBE_shared_dota_lobby_state` 需要先继续收敛 publish/restore/metadata 入口，再评估 `DotaGcRuntimeState`
    - 盘点命令：`rg -n --glob '*.{h,cpp}' 'GBE_shared_dota_lobby_state' dll`；`rg -n --glob '*.{h,cpp}' 'GBE_recent_dota_reconnect_context|GBE_dota_reconnect_eligible|GBE_GetDotaReconnectContext|GBE_TryRecoverDotaReconnectContextFromGenericLobbies|GBE_IsDotaArcadeLobbyActive' dll`；`rg -n --glob '*.{h,cpp}' 'GBE_pending_reset_after_cache_unsubscribed|GBE_pending_dota_abandon_finalize|GBE_pending_dota_normal_signout_finalize|GBE_suppressed_dota_abandon_lobby_id' dll`；`rg -n --glob '*.{h,cpp}' 'GBE_dota_login_sync_sent|GBE_dota_private_lobby_snapshot_replayed|GBE_last_dota_launch_state_pushed_game_state|GBE_last_lobby_poll_time|GBE_last_dota_launch_persona_signature|GBE_last_dota_direct_connect_callback_signature|GBE_last_dota_server_hello_context|GBE_dota_host_showcase_equip_pushed' dll`；`rg -n --glob '*.{h,cpp}' 'GBE_vpk_loot_data|GBE_last_post_connection_state|GBE_post_connection_state_retry_count|GBE_last_direct_connect' dll`
    - 验证：`git diff --check` 通过
  - [x] 9.2 为 pending flow state 建立函数级访问接口
    - 先封装 abandon reset、normal signout finalize、postgame teardown 标志
    - 调用方通过函数读写，不直接访问 extern global
    - 结果：在 `Steam_Game_Coordinator` 上新增 pending flow state 函数级访问接口，封装 `GBE_pending_reset_after_cache_unsubscribed`、`GBE_pending_dota_abandon_finalize_after_7014`、`GBE_pending_dota_normal_signout_finalize_after_25` 及其 lobby id 字段的 set/has/consume/clear 操作；7035 current-game/failed launch abandon、7272 abandon channel leave、7004 signout、postgame teardown queue、`ResetGCMemory`、`clear_dota_runtime_state` 和 `RetrieveMessage` 均改为通过函数读写
    - 行为保持：pending flag 存储位置仍保留在 coordinator 成员字段，`RetrieveMessage` 仍在 pop 后按原顺序执行 abandon finalize、normal signout finalize 和 deferred reset；postgame teardown queue 继续保留 pending reset 的 lobby id 记录但不标记 active pending
    - 测试支架：handler test stub 增加同名访问接口，`test_lobby_abandon_current_game_disconnect_queues_25` 改为通过函数检查 pending reset active 状态
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 9.3 为 reconnect state 建立函数级访问接口
    - 封装 recent reconnect context 和 reconnect eligibility
    - 保持现有 payload helper 与 lobby state helper 行为
    - 结果：在 `dll/gbe_dota_reconnect_shared.h` 增加 reconnect state 函数级访问接口，封装 recent reconnect context 的 get/set/clear，以及 reconnect eligibility 的 load/store/consume；`GBE_recent_dota_reconnect_context_valid`、`GBE_recent_dota_reconnect_context` 和 `GBE_dota_reconnect_eligible` 继续定义在 `steam_game_coordinator.cpp`，外部 TU 不再直接访问 extern 状态
    - 行为保持：`GBE_GetDotaReconnectContext` 仍优先使用 shared lobby state，再 fallback recent context；`GBE_PublishSharedDotaLobbyState` 仍在可构造 reconnect context 时更新 recent cache；custom lobby create 和 runtime clear 仍按原语义清理 recent cache；`SendP2PRendezvous` 仍通过 compare-exchange 一次性消费 reconnect eligibility；`steam_user.cpp` 的 auth ticket reconnect reset 语义保持不变
    - 测试支架：payload helper test 和 handler free-function stubs 增加同名 reconnect state accessor，保持离线测试链接入口与生产声明一致
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 9.4 评估 `DotaGcRuntimeState` 结构体迁移门槛
    - 至少两个状态组稳定通过函数访问后再考虑结构体封装
    - 不一次性迁移所有 globals
    - 结果：当前已满足进入评估的前置条件，9.2 的 pending flow state 和 9.3 的 reconnect state 已稳定通过函数级访问接口，并通过 full offline tests、审计和 `git diff --check`
    - 迁移结论：暂缓实际引入 `DotaGcRuntimeState` 结构体；先继续按状态组封装访问接口，再在更低风险范围内抽取结构体。当前 `GBE_local_lobby` 和 `GBE_shared_dota_lobby_state` 读写扇出仍高，直接迁入结构体会让 create/join/launch/post-login/reconnect/template replay 等路径同时变更，风险超过本阶段收益
    - 建议门槛：下一次考虑结构体迁移前，至少再完成 launch/sync flags 或 host showcase flag 的函数级访问封装；`GBE_local_lobby` 需要先收敛 create/join/restore/publish/details update 的主要写入口；`GBE_shared_dota_lobby_state` 需要先保留在 publish/restore/metadata helper 后方，避免 payload helper 和 networking 路径直接依赖结构体布局
    - 可行切入：优先定义小型 internal storage struct 仅承载 pending flow 与 reconnect cache 这类已封装状态，保持 accessor API 不变；等第三个状态组也完成函数访问后，再评估是否把该 storage 扩展为 `DotaGcRuntimeState`
    - 验证：`git diff --check` 通过

- [x] 10. Protocol codec DTO 化
  - [x] 10.1 定义 7034 runtime request DTO
    - 覆盖 connected/disconnected players、game_state、send_reason、kill/building state
    - parse 逻辑不读取 coordinator 或 global state
    - 结果：新增 `gbe::proto_wire::Dota7034RuntimeRequest` 和 `parse_dota7034_runtime_request`，复用既有 wire shape parser 拷贝字段，保持 pure parse 边界；active-lobby runtime mutation、owner team/slot/hero、disconnected-player、strategy-time fallback helper 改为使用 runtime request DTO
    - 行为保持：`GBE_HandleDotaDirect7034Response` 继续使用 `Dota7034RequestShape` 构造响应，避免本阶段改动 response adapter 语义；7034 side-effect 顺序保持 runtime mutation/update 先于 7034 response
    - 测试：新增 `test_parse_dota7034_runtime_request`，覆盖 connected/disconnected players、game_state、send_reason、first blood、radiant/dire kills、radiant lead、building state 和 draft owner team/slot 字段
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 10.2 定义 7070/8052/8053 launch DTO
    - 覆盖 ready state、lobby id、custom game id、start time、duration、result code、result text
    - handler 使用 DTO 字段替代裸 `read_uint*_field` 分散读取
    - 结果：新增 `Dota7070ReadyUpRequest`、`Dota8052StartedLoadingRequest`、`Dota8053FinishedLoadingRequest` 以及对应 parse helper；`parse_dota8053_result` 保留为兼容 wrapper，内部复用 finished-loading DTO
    - handler 接入：`GBE_HandleDotaCustomGameReadyUpRequest`、`GBE_HandleDotaCustomGameStartedLoadingRequest`、`GBE_HandleDotaCustomGameFinishedLoadingRequest` 改为读取 DTO 字段，去除 handler 内分散的 7070/8052 裸 `read_uint*_field` 调用
    - 测试：扩展 `gbe_proto_wire_test` 覆盖 7070 ready_state、8052 lobby/custom/start time、8053 duration/result code/result text/signon states 与 `has_*` 标记；既有 handler 顺序测试继续覆盖 7170、8052、8053 runtime/publish 副作用
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 10.3 定义 7035 abandon request context DTO
    - 覆盖 wrapped、session、server/client、lobby state、game state、launch phase
    - 作为 transition decision 输入
    - 结果：新增 `DotaAbandonRequestContext` 和 `build_dota_abandon_request_context`，捕获 wrapped、wrapped session、server/client、owner connected、custom game、lobby id/state/game_state/server_id/launch_phase/postgame chat channel 等 transition 输入；新增 `compute_abandon_decision(const DotaAbandonRequestContext&)`，旧 lobby/wrapped/server 签名保留为兼容 wrapper
    - handler 接入：`GBE_HandleDotaAbandonCurrentGameRequest` 先构造 context，再将 context 传给 abandon decision；wrapped session 缺失判断收口到 decision 的 `require_wrapped_session`
    - 测试：扩展 `gbe_dota_lobby_state_test`，覆盖 7035 context 字段捕获、wrapped session present/missing 对 decision 的影响，以及 inactive lobby context 构造失败
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - [x] 10.4 为 DTO parse 添加 malformed wire tests
    - 覆盖空 body、truncated varint、unknown wire type、字段重复
    - 结果：扩展 `gbe_proto_wire_test`，覆盖 7070、8052、8053 和 7034 runtime DTO parser 的空输入、截断 varint、非法 wire type、截断 length-delimited nested message 与重复字段行为
    - 行为锁定：标量字段沿用 `find_field` 的首个匹配字段语义；7034 repeated connected-player 字段继续累积；malformed 字段保持字段缺省而不触发 handler 副作用
    - 验证：`tools/run_gc_offline_tests.sh --full` 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过

- [x] 11. Verification pipeline 固化
  - [x] 11.1 新增本地 pre-merge checklist 文档
    - 记录 fast/full/style/audit/Premake gate
    - 放在 `.monkeycode/specs/gc-refactor-next-steps/` 或 `.monkeycode/docs/`
    - 结果：新增 `.monkeycode/specs/gc-refactor-next-steps/pre-merge-checklist.md`，记录 fast gate、full gate、audit gate、style gate、Premake gate 的命令、适用场景和完成规则
    - 完成规则：GC refactor task 标记完成前运行 `tools/run_gc_offline_tests.sh --full`、`python3 tools/_audit_gc_refactor.py`、`git diff --check`，并在 tasklist 中记录结果
    - 验证：`git diff --check` 通过
  - [x] 11.2 将验证命令收口到脚本或 Make target
    - 保留 `tools/run_gc_offline_tests.sh` 作为主入口
    - 增加可选 audit/style wrapper 时避免破坏现有脚本行为
    - 结果：新增 `tools/run_gc_verification.sh` wrapper，默认运行 full offline tests、audit 和 `git diff --check`；支持 `--fast`、`--full`、`--skip-audit`、`--skip-style`
    - 行为保持：`tools/run_gc_offline_tests.sh` 未改动，仍是离线测试主入口；wrapper 仅编排现有测试、审计和 style gate
    - 文档：更新 pre-merge checklist，记录 wrapper 的 full/fast 用法
    - 验证：`tools/run_gc_verification.sh --fast` 通过
  - [x] 11.3 评估 CI 接入条件
    - 只包含离线测试和无需凭据的检查
    - Premake gate 使用 `--with-gc-tests` 并在 CI 镜像具备工具时启用
    - 结果：新增 `.monkeycode/specs/gc-refactor-next-steps/ci-verification-evaluation.md`，记录 CI-safe checks、建议触发路径、Premake gate 条件、暂缓项和推荐接入方式
    - 结论：首个 PR CI pass 建议运行 `tools/run_gc_verification.sh --fast`；pre-merge/scheduled CI 运行完整 `tools/run_gc_verification.sh`；Premake gate 作为独立可选 job，确认 CI 镜像具备 `premake5` 后启用
    - 验证：`tools/run_gc_verification.sh` 通过

- [x] 12. Template/Replay 数据治理
  - [x] 12.1 盘点 template/replay 常量 ownership
    - 覆盖 `gbe_dota_template_replay_handlers.cpp` 和 payload helper 中的 canned bytes/hex 常量
    - 为 client welcome、server welcome、practice lobby cache subscribed、persona state、official 26 replay 分类
    - 结果：新增 `.monkeycode/specs/gc-refactor-next-steps/template-replay-ownership.md`，记录 direct legacy replay、payload helper、launch persona、private-lobby fallback persona 的 owner，并按 client welcome、server welcome、practice lobby cache subscribed、persona state、official 26 replay 分类
    - 规则：新增大型 canned bytes 放入最窄 owner；handler 优先调用 helper，不直接持有可复用大 hex/blob
  - [x] 12.2 标注文档化 patch 点
    - 记录 account id、steam id、lobby id、match id、owner SOID、game start time 等 patch 字段
    - 优先使用代码附近注释或小型 metadata 表达，不引入大型 registry
    - 结果：在 `gbe_proto_wire.h` 的 template patch result 定义旁记录 practice lobby launch template 与 peripheral/persona template patch points，包括 required/optional 和 size-checked 替换语义；ownership 文档记录 cache subscribed、official 26、persona state 等共享 patch 边界
  - [x] 12.3 增加 template patch focused tests
    - 覆盖 patch 后关键字段可解析
    - 保持现有 replay fixture 输出不变
    - 结果：扩展 `gbe_proto_wire_test` 中 practice lobby launch/peripheral template patch 用例，在原有 match count、size guard 和 bytes 断言基础上，解析验证 SteamID fixed64、lobby id、match id、server id、game start time、connect string、lobby id text 的 patch 后值
    - 验证：`tools/run_gc_verification.sh --fast` 通过，包含 replay fixture 对齐和 `git diff --check`
  - [x] 12.4 防止新 handler 复制 hex blob
    - 新增 template/replay 数据时优先放在 template/replay 或 payload helper 领域
    - handler 通过 helper 调用，不直接持有大型 canned bytes
    - 结果：扩展 `tools/_audit_gc_refactor.py`，新增 template/replay canned blob ownership audit；允许 `gbe_dota_template_replay_handlers.cpp` 和 `gbe_dota_gc_payload_helpers.cpp` 作为大型 canned hex owner，普通 `gbe_dota_*_handlers.cpp` 中出现长 hex literal 会失败并提示移动到 template/replay 或 payload helper 边界
    - 验证：`python3 tools/_audit_gc_refactor.py` 通过

- [x] 13. Logging/Trace 边界治理
  - [x] 13.1 盘点高风险 reason string
    - 覆盖 inventory equip、chat leave、lobby abandon、match 7034、7070、8052、8053
    - 输出保留、重命名、补断言清单
    - 结果：新增 `.monkeycode/specs/gc-refactor-next-steps/reason-trace-governance.md`，记录高风险 reason inventory、保留/重命名结论和测试覆盖状态；7035 current-game response 记录为直接 push 路径，无 action reason metadata，`7035_current_game_disconnect` 由 abandoned-lobby suppression 使用
  - [x] 13.2 统一新增 reason 命名规则
    - 使用 `emsg_or_flow_event` 风格
    - reason 表达业务触发点，避免绑定临时代码结构
    - 结果：治理文档记录新增 reason 命名规则：单请求路径以 emsg 开头，跨消息生命周期使用 flow name，表达业务触发点或协议事件，保持 reason 稳定并同步更新测试和 inventory
  - [x] 13.3 梳理日志函数职责边界
    - `GBE_GC_DebugLog` 保留上下文调试输出
    - `GBE_LogDotaResponsePacket` 保留 outbound response 观察
    - proto boundary trace 保留 wire 层边界观察
    - 结果：治理文档记录 trace boundary：`GBE_GC_DebugLog` 负责上下文诊断，`GBE_LogDotaResponsePacket` 负责 outbound response 观察，proto boundary trace 保持在 wire/helper 边界，recorded action/publish reason 作为测试稳定语义标签
  - [x]* 13.4 增加 reason stability tests
    - 对高风险 handler 的关键 action reason 做断言
    - 避免重构后日志语义漂移
    - 结果：扩展 handler smoke tests，补充 8053 load failure 分支 reason stability 覆盖，并保留 7035 current-game cache-unsubscribe 行为断言；现有测试已覆盖 equip、chat leave、7040、7081、7046、7035 postgame、8879、7070、8052、8053 success、7034 connected/disconnected/game_state/launch_poll 等高风险 reason
    - 验证：`tools/run_gc_verification.sh --fast` 通过

- [x] 14. 平台和构建等价性治理
  - [x] 14.1 盘点 GC 相关源列表入口
    - 覆盖 `tools/run_gc_offline_tests.sh`、`premake5.lua`、生产 project 和 workflow matrix
    - 记录新增 `.cpp` 应进入哪些入口，测试专用 `.cpp` 只进入可选 test target
    - 结果：新增 `.monkeycode/specs/gc-refactor-next-steps/build-entrypoint-equivalence.md`，记录 production Premake `common_files`、shell offline tests、always-on utility tests、`--with-gc-tests` heavy tests、Linux/Windows workflow matrix 的 GC source inclusion 规则；新增生产 `.cpp` 进入 `dll/` 后自动进入生产 Premake，但必须手动同步 shell tests 和可选 test target source list
  - [x] 14.2 验证默认 Premake 路径保持轻量
    - 默认 `premake5 gmake2` 不生成 GC test targets
    - `premake5 --with-gc-tests gmake2` 生成两个 GC test targets
    - 结果：`premake5.lua` 中 heavy GC handler/payload tests 仍由 `if _OPTIONS["with-gc-tests"] then` 守护；`tool_gbe_dota_gc_payload_helpers_test` 与 `tool_gbe_dota_handler_test` 只在 `--with-gc-tests` 路径生成，默认 workflow 生成命令没有传该选项
    - 环境限制：当前 PATH 无 `premake5`，未执行实际 project generation；已通过读取 `premake5.lua` 和 workflow 命令验证配置结构
  - [x] 14.3 维护 Windows/Linux 构建差异清单
    - 盘点路径、宏、include 顺序、链接顺序相关风险
    - 涉及跨平台宏时补充对应离线或 CI 验证说明
    - 结果：构建等价性文档记录 Linux `gmake` 与 Windows `vs2026` generator 差异、production source inclusion、Linux-only shell offline gate、include 顺序和 link/source list 风险；本轮未引入跨平台宏或源列表变更
  - [x]* 14.4 评估 PR CI smoke gate
    - 优先接入无需凭据的 GC offline fast gate
    - 保持默认 project matrix 构建时间可控
    - 结果：建议首个 PR gate 使用 `tools/run_gc_verification.sh --fast`，pre-merge/scheduled 使用完整 `tools/run_gc_verification.sh`；Premake GC test generation 保持单独可选 job，等待 CI 镜像确认 premake5 和依赖目录可用

- [x] 15. 依赖 seam、持久化和错误边界治理
  - [x] 15.1 盘点 handler 外部依赖触点
    - 覆盖 `Steam_Client`、network broadcast、server GC forward、settings、item save、lobby publish
    - 按 pure planner 输入、executor 副作用、global runtime state 三类记录
    - 结果：新增 `.monkeycode/specs/gc-refactor-next-steps/dependency-seam-error-boundary.md`，按 Settings、Steam_Client、network broadcast、server GC forward、item persistence、lobby publish、global runtime state 分类记录触点和治理规则
  - [x] 15.2 建立函数级 dependency seam
    - 先封装 item save、server GC forward、network broadcast、lobby publish 等高风险副作用
    - seam 记录调用参数并保持显式顺序
    - 结果：新增 `Steam_Game_Coordinator::GBE_SaveDotaItemsFromExecutor(reason)` 作为 Dota executor persistence seam，当前 equip handler 在原 SaveItemsToFile 顺序点调用该 seam；seam 记录 reason 并委托既有 `save_items_to_file()`；handler test coordinator stub 同步声明该 seam，保持既有 SaveItemsToFile recorder action 顺序可见
  - [x] 15.3 明确数据 ownership 和生命周期
    - 覆盖 shared lobby、item cache、recent reconnect context、pending flags、template replay bytes
    - 记录线程访问假设和不得跨 action 持有的引用
    - 结果：治理文档记录 `GBE_local_lobby`、`items`、reconnect context、pending flags、template/replay bytes 的 ownership 和生命周期约束；纯 helper 使用快照或标量输入，禁止跨 queued action 持有引用
  - [x] 15.4 收口 persistence 触发点
    - 确认 save 只在 executor/coordinator 层触发
    - pure decision/helper 返回保存意图，不直接写文件
    - 结果：治理文档记录 persistence boundary，pure parser/DTO/decision helper 只返回 intent，保存由 coordinator/executor 层通过命名 reason seam 触发；本轮 equip planner 仍只输出 `SaveItemsToFile` action intent
  - [x] 15.5 建立错误分类和 fallback 清单
    - 覆盖 parse failure、missing lobby、missing item、server GC unavailable、template patch failure
    - 对每类错误明确 response、log、state mutation 和 return value 语义
    - 结果：治理文档记录 parse failure、missing lobby、missing item、server GC unavailable、template patch failure 的 response/log/state mutation 语义
  - [x] 15.6 建立 replay/template 性能和 fixture 版本治理
    - 为大型 payload/template 避免重复拷贝设定局部约束
    - 为 canned payload 添加来源、用途或版本注释
    - 变更 fixture 时同步 focused tests
    - 结果：治理文档记录 replay/template 性能规则：大型 blob 保持 helper ownership，patch helper 返回 metadata，fixture 变更同步 replay expected files 和 focused tests
    - 验证：`tools/run_gc_verification.sh --fast` 通过；`tools/run_gc_verification.sh` 通过；`git diff --check` 通过

- [x] 16. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户
  - 执行 `tools/run_gc_offline_tests.sh`
  - 执行 `tools/run_gc_offline_tests.sh --full`
  - 执行 `git diff --check`
  - 涉及声明迁移时执行 `python3 tools/_audit_gc_refactor.py`
  - 检查 `git status --short`，确认只包含本轮目标文件变更
  - 结果：默认 GC offline tests 通过；full GC offline tests 通过；`python3 tools/_audit_gc_refactor.py` 通过；`git diff --check` 通过
  - 审查修复：修复 `GBE_ClearPendingDotaNormalSignoutFinalizeAfterCacheUnsubscribed` 自递归回归，新增 `test_lobby_normal_signout_pending_clear_resets_state`，handler smoke tests 增至 36 个
  - 复验：`tools/run_gc_verification.sh --fast` 通过；`tools/run_gc_verification.sh` 通过
  - 状态检查：`git status --short` 显示当前工作区仍包含 GC 重构累计 WIP、文档、opencode 配置和验证脚本变更；未发现需要回滚的无关文件
